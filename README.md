# セキスペ・セキュリティ・ラボ

情報処理安全確保支援士（セキスペ）の試験範囲で頻出する暗号・認証技術、および各種ネットワーク防衛技術を、実際のデータ・通信ログを見ながら体系的かつ体験的に学習するためのインタラクティブなローカルラボ環境です。

## 特徴
- **対話型シミュレーション**: パスワードのハッシュ化速度、JWT改ざん検証、TOTP（MFA）時間同期、OAuth 2.0認可シーケンス、RSA暗号・デジタル署名の改ざん検知、境界防衛、Web脆弱性、Kerberosチケット交換、IPsecパケットカプセル化などをブラウザ上で直感的に試せます。
- **攻撃手法クイック・リファレンス（NEW）**: ダッシュボード下部に、セキスペ頻出の各種攻撃手法（辞書攻撃、JWT改ざん、CSRF、SQLi、XSS、リプレイ攻撃、ポートスキャン等）の概要・防衛策・試験ポイントをまとめたインタラクティブなアコーディオン（アコーディオンドロップダウン）を実装。そこから直接関連する学習ラボへジャンプできます。
- **セキスペ用語検索（簡易事典機能）**: 各モジュールの上部から、シラバス準拠のセキュリティ/認証重要用語（全52語）をリアルタイムに検索可能。試験での重要出題ポイントをその場で確認できます。
- **ドラッグリサイズ対応ライブ通信ログ（UPDATE）**: 画面下部の「APIログ＆パケットコンソール」にて、送受信される実際のHTTPリクエスト/レスポンス、セキュリティヘッダー、暗号キー、パケット内容をリアルタイム表示。**上部の境界線をドラッグすることで高さを自由に調節可能**になり、背後のメイン画面の余白も自動同期されます。
- **高い拡張性**: 新しい学習テーマを、プラグインのように簡単に追加できるモジュール化設計になっています。
- **軽量かつ自己完結**: Node.jsや外部データベースに依存せず、Python 3と標準的なセキュリティライブラリのみで動作します。

---

## クイックスタート

### 動作要件
- Python 3.12 以上
- `uv` (高速パッケージマネージャー)

### 1. サーバーの起動
以下のコマンドを実行して、ローカルサーバーを起動します。
ポートは安全な **`18000`** を使用します。

```bash
# 仮想環境が未作成またはライブラリが未導入の場合は以下を実行
uv venv
uv pip install -r requirements.txt

# サーバー起動 (自動リロード有効)
.venv/bin/uvicorn main:app --host 127.0.0.1 --port 18000 --reload
```

### 2. ブラウザでアクセス
以下のURLにアクセスすると、セキュリティ・ラボのダッシュボードが表示されます。
👉 **[http://127.0.0.1:18000](http://127.0.0.1:18000)**

---

## 搭載されている学習モジュール (全9モジュール)

1. **パスワードハッシュ化＆ソルト (Password Hashing)**
   - SHA-256（ソルトなし・高速）と bcrypt（ソルト自動付与・意図的な遅延処理）の違いを比較。
   - 辞書攻撃を実行して、SHA-256ハッシュがどれだけ一瞬で突破されるかを検証。
2. **JWTセキュリティ (JWT Security)**
   - JWTのヘッダー、ペイロード、署名の構造を分解表示。
   - ペイロードの改ざん（権限の書き換え）や `alg: "none"` 脆弱性を突いたリクエストをサーバーに送信し、改ざん検知や保護ロジックを学習。
3. **多要素認証 (MFA) & TOTP**
   - 共有鍵（Base32）から Unix Time を用いた30秒おきのOTP計算プロセスを可視化。
   - QRコードをスマホの Authenticator アプリで読み取って同期できるリアルな検証機能。
4. **OAuth 2.0 / OIDC フロー (OAuth Flow)**
   - 認可コードフロー（フロントチャネル・バックチャネル）のステップ実行。
   - `state` パラメータを除外した際の CSRF 脆弱性の成立過程のビジュアライズ。
5. **暗号化＆デジタル署名 (Cryptography)**
   - 共通鍵（AES-256-GCM）での暗号化・復号（改ざん検知）。
   - RSA鍵ペア生成、公開鍵暗号、および秘密鍵によるデジタル署名の作成と、1文字でも改ざんされると署名が通らなくなる仕組みの検証。
6. **FW / IDS・IPS / WAF 使い分け (Network Defense)**
   - 境界防衛ネットワークシミュレータを操作し、パケット（ポートスキャン、OSコマンド実行、SQLi/XSS、User-Agent等）がどのレイヤー（L4、L7シグネチャ、L7 HTTPコンテキスト）で遮断・検知されるかをアニメーション可視化。
7. **Web脆弱性攻撃と防衛 (Web Vulnerability)**
   - 代表的なWeb脆弱性である SQL インジェクション（SQLi）とクロスサイトスクリプティング（XSS）の動作原理。
   - **「静的プレースホルダ（バインド変数）」** や **「HTMLエスケープ（サニタイズ）」** によるセキュアプログラミングの根本対策を学習。
8. **認証プロトコル・ログ解析 (Kerberos)**
   - Kerberos（ケルベロス）認証の3つのチケット発行フェーズ（AS、TGS、AP）のステップ実行。
   - 暗号化された **TGT（チケット交付チケット）** やサービスチケットの内容、および **リプレイ攻撃対策としてのタイムスタンプ** を含む生ログのデコード解析。
9. **IPsec構造・IKE交換 (IPsec Deep Dive)**
   - 動作モード（トンネル / トランスポート）とプロトコル（AH / ESP）に応じた **IPsecパケットカプセル化レイアウトのビジュアル構築**。
   - IKE Phase 1（メインモード/アグレッシブモード）と Phase 2 の鍵ネゴシエーションフローのメッセージログシミュレーション。

---

## 新しい学習モジュールの追加方法（拡張手順）

本システムは、新たなセキュリティ検証モジュールを容易に拡張できるよう設計されています。

1. **バックエンド API の追加** (`main.py`):
   - 必要なビジネスロジックやAPIエンドポイント（例: `/api/new-feature/*`）を `main.py` に追加します。
   - `main.py` 内の `MODULES` リストに新モジュールの定義（`id`, `title`, `description`, `jsFile`）を追記します。
     ```python
     {
         "id": "new_feature",
         "title": "新しいセキュリティ検証",
         "description": "...",
         "jsFile": "lab_new_feature.js"
     }
     ```

2. **フロントエンド JavaScript の作成** (`static/js/lab_new_feature.js`):
   - `static/js/` 配下に指定した名前のファイルを作成し、UIとなる HTML と動作スクリプトを登録します。
     ```javascript
     window.SecurityLabModules["new_feature"] = {
         html: `
             <div class="card">
                 <h3>🛡️ 新しいセキュリティラボ</h3>
                 <button class="btn btn-primary" id="btnTest">テスト</button>
             </div>
         `,
         init: function(app) {
             document.getElementById("btnTest").addEventListener("click", () => {
                 app.log('system', '新モジュールのボタンがクリックされました！');
             });
         }
     };
     ```

これで、サーバー起動時に自動的にダッシュボードとサイドメニューに新しいモジュールが読み込まれ、タブを切り替えて実験を行うことができます。
